<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.oneyi.mapper.ActRuTaskMapper">
<!-- 根据角色权限名查询任务 -->
    <select id="selectByassignee" resultType="java.lang.String">
        select PROC_INST_ID_ from act_ru_task where ASSIGNEE_= #{userRole}
    </select>

    <select id="list" resultType="java.util.Map">
        select t.NAME_,t.ASSIGNEE_,t.OWNER_,c.MESSAGE_,c.TYPE_,t.START_TIME_ from act_hi_taskinst as t left join act_hi_comment as c on t.ID_ = c.TASK_ID_
    </select>
</mapper>